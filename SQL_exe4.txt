SQL Exercise 4 

1. Display the minimum Status in the Supplier table. 
Ans -:select min(status) status from supplier;

2. Display the maximum Weight in the Parts table. 
Ans -:select max(weight) Weight from parts;

3. Display the average Weight of the Parts.
Ans -:select avg(weight)  from parts; 

4. Display the total Quantity sold for part ‘P1’. 
Ans -:select sum(qty) from SPJ where `P#`='p1';

5. Display the total Quantity sold for each part.
Ans -:select `p#`,sum(qty) from SPJ 
	group by `p#`;

6. Display the average Quantity sold for each part.
Ans -:select `p#`,avg(qty) from SPJ
	group by `p#`;

7. Display the maximum Quantity sold for each part, provided the maximum Quantity
is greater than 800. 
Ans -:select `P#`,max(qty) from SPJ
	where qty > 800 group by `P#`;

8. Display the Status and the count of Suppliers with that Status
Ans -:select status,count(status) from supplier group by status;

9. Display the count of Projects going on in different cities. 
Ans -:	1)select city,count(city) from projects group by city;
	2)select city , count(*) from projects group by city ;


10. What is the difference between COUNT(Status) and COUNT(*) ? 
Ans -:	count(status)-It will count total no.of status in coloumn;
	count(*)- It will count total no of row in table;

11. Display the Status and the Count of Suppliers with that Status in the following format
as shown below:-

Status Count
Ten 1
Twenty 2
Thirty 3 

Ans -:select case 
	when status=10 then 'Ten'
    	when status=20 then 'Twenty'
    	when status=30 then 'Thirty'
    	end Status ,count(status) as count
	from supplier group by status;